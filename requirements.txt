# --- UI ---
PySide6==6.8.2
pyqtgraph==0.13.7

# --- Supabase / Backend ---
# >=2.10 e necessario para suportar as novas chaves de API do Supabase
# (formato sb_publishable_/sb_secret_) - versoes anteriores validam a chave
# com uma regex que exige o formato JWT antigo e rejeitam as novas chaves.
supabase==2.31.0
python-dotenv==1.0.1

# --- Segurança (armazenamento de sessão) ---
keyring==25.3.0

# --- Cálculos e utilidades ---
python-dateutil==2.9.0.post0
tzdata==2024.1

# --- Relatórios ---
openpyxl==3.1.5
reportlab==4.2.2

# --- Testes ---
pytest==8.3.2
pytest-qt==4.4.0
